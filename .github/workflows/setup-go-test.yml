name: setup-go-test

on:
  workflow_dispatch:
  push:

jobs:
  go-action-test:
    runs-on: ubuntu-22.04-ppc64le
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v4
        with:
          go-version: '1.21'
          architecture: 'ppc64le'
      - run: |
          go version
